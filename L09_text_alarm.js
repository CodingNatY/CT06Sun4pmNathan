// let speed = 3;
// let x=50;
// let y=200;
function setup(){
    createCanvas(600,400);
}
function draw(){
    background(220);
    // fill(0);
    // textSize(32);
    // text("bounce",x,y);
    // x+=speed;
    // if(x<0 || x>600-60){
    //     speed = -speed;
}
